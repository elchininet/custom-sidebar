---
title: Changing options when navigating to a panel
slug: changing-options-when-navigating-to-a-panel
authors: elchininet
tags: [sidebar, home-assistant]
hide_table_of_contents: true
---

It is possible to change `Custom Sidebar` options when navigating through Home Assistant panels. You can dinamically change all the options that accept a template to react to changes in the Home Assistant URL.

{/* truncate */}

import Example from './_partials/2024-12-05-changing-options-when-navigating-to-a-panel/_example.mdx';

To make use of this feature you need to use [JavaScript templates](../templates/javascript-templates) and use the special variable `panel_url`. This variable is dynamic and will make the template get re-evaluated every time that the panel URL changes.

## panel_url

In the next example, the option `sidebar_background` is using a `JavaScript` template that will return a different value depending on the `panel_url` variable.

<Example />

<figure>
![Mobile device with sidebar_mode in narrow](./_images/2024-12-05-changing-options-when-navigating-to-a-panel/sidebar-background-changing-when-panel-url-changes.gif)
<figcaption>_Several `Custom Sidebar` options changing depending on the `panel_url` variable_</figcaption>
</figure>