```yaml title="custom-sidebar-config.yaml"
jinja_variables:
  admin_boolean: 'input_boolean.show_admin_sidebar_items'
partials:
  admin_active: '{% set admin_active = is_state(admin_boolean, "on") %}'
order:
  - item: developer tools
    hide: |
      @partial admin_active
      {{ admin_active }}
  - item: studio code server
    hide: |
      @partial admin_active
      {{ admin_active }}
  - item: settings
    hide: |
      @partial admin_active
      {{ admin_active }}
```
