```typescript
openAlertDialog({
  // The title of the dialog
  title: string;
  // The text of the dialog
  text: string;
  // The text of the confirm button
  confirmText?: string;
  // Function that will be executed when the confirm button is clicked
  confirm?: () => void;
})
```