```yaml title="custom-sidebar-config.yaml"
order:
  - new_item: true
    item: 'Restart'
    icon: 'mdi:restart'
    on_click:
      action: 'javascript'
      code: |
        openRestartDialog();
```
